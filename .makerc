
alias make='bear_make'

bear_make() {
    local ec=0
    if [[ ! "$1" =~ ^run.*$ ]]; then
        echo "bear --append -- make:"
        bear --append -- /usr/bin/make "$@"
        ec=$?
    else
        /usr/bin/make "$@"
        ec=$?
    fi
    return $ec
}